name: Build MOTSMART Firmware (ci_build/full)

on:
  push:
    tags:
      - 'v*'                 # build saat push tag, mis. v0.3.5-safeoff
  workflow_dispatch:         # bisa dijalankan manual dari tab Actions

permissions:
  contents: write            # dibutuhkan jika bikin Release

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Cache PlatformIO & pip
        uses: actions/cache@v4
        with:
          path: |
            ~/.platformio/.cache
            ~/.cache/pip
          key: pio-${{ runner.os }}-${{ hashFiles('**/platformio.ini') }}
          restore-keys: |
            pio-${{ runner.os }}-

      - name: Install PlatformIO
        run: pip install platformio

      - name: Clean build folder (env full)
        working-directory: ci_build
        run: pio run -e full -t clean

      - name: Build (env full) with SAFE_MODE=0 via build_flags
        working-directory: ci_build
        run: |
          # JANGAN pakai `pio run -D ...` (itu salah). Gunakan -O/--project-option:
          # build_flags=${build_flags} -D SAFE_MODE=0 ==> menambahkan macro ke build_flags yang sudah ada
          pio run -e full -O 'build_flags=${build_flags} -D SAFE_MODE=0'

      - name: List built files (debug)
        working-directory: ci_build
        run: |
          echo ">>> Tree of .pio/build/full"
          ls -alR .pio/build/full || true
          echo ">>> flasher_args.json (jika ada)"
          if [ -f .pio/build/full/flasher_args.json ]; then
            cat .pio/build/full/flasher_args.json
          else
            echo "flasher_args.json not found."
          fi

      - name: Upload artifacts (whatever exists)
        uses: actions/upload-artifact@v4
        with:
          name: motsmart-full
          path: |
            ci_build/.pio/build/full/*.bin
            ci_build/.pio/build/full/flasher_args.json
          if-no-files-found: warn

      # OPTIONAL: merge ke satu berkas hanya jika semua bagian ada
      - name: Create merged image (optional)
        run: |
          BOOT=$(ls ci_build/.pio/build/full/bootloader*.bin 2>/dev/null | head -n1 || true)
          PART=$(ls ci_build/.pio/build/full/partitions*.bin 2>/dev/null | head -n1 || true)
          APP="ci_build/.pio/build/full/firmware.bin"
          if [ -n "$BOOT" ] && [ -n "$PART" ] && [ -f "$APP" ]; then
            echo "Merging:"
            echo "  BOOT: $BOOT"
            echo "  PART: $PART"
            echo "  APP : $APP"
            pip install esptool
            # gunakan perintah baru: merge-bin (bukan merge_bin)
            esptool.py --chip esp32 merge-bin -o ci_build/.pio/build/full/merged_full.bin \
              0x1000 "$BOOT" 0x8000 "$PART" 0x10000 "$APP"
          else
            echo "Skip merge: one or more files missing."
          fi

      - name: Upload merged image (if created)
        if: ${{ hashFiles('ci_build/.pio/build/full/merged_full.bin') != '' }}
        uses: actions/upload-artifact@v4
        with:
          name: motsmart-full-merged
          path: ci_build/.pio/build/full/merged_full.bin
          if-no-files-found: warn

      # OPTIONAL: buat GitHub Release saat push tag dan lampirkan file yang ada
      - name: Collect release assets
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          mkdir -p ci_build/.pio/build/full/release
          for f in bootloader*.bin partitions*.bin firmware.bin merged_full.bin flasher_args.json; do
            [ -f "ci_build/.pio/build/full/$f" ] && cp "ci_build/.pio/build/full/$f" "ci_build/.pio/build/full/release/"
          done
          ls -al ci_build/.pio/build/full/release || true

      - name: Create GitHub Release & upload assets
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: ci_build/.pio/build/full/release/*
          draft: false
          prerelease: false      # OPSIONAL: Rilis otomatis saat push tag
      - name: Create GitHub Release (attach binaries)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.REF_NAME }}
          name: Release ${{ env.REF_NAME }}
          draft: false
          prerelease: false
          files: |
            ci_build/.pio/build/full/firmware.bin
            ci_build/.pio/build/full/bootloader*.bin
            ci_build/.pio/build/full/partitions*.bin
            ci_build/.pio/build/full/merged_full.bin
