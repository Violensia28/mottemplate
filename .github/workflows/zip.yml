- name: Unzip source
  run: |
    sudo apt-get update && sudo apt-get install -y unzip
    unzip -q motsmart_template.zip -d extracted
    shopt -s dotglob
    mv extracted/*/* .
    rm -rf extracted
